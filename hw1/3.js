//Перепишите код, заменив оператор `switch` на оператор `if..else`:

let val = 'c';
if (val == 'a') {
    console.log(val);
}
else if (val === 'b' || val === 'c' || val === 'd' || val === 'e') {
    console.log('Others');
} else {
    console.log('unknown');
}

